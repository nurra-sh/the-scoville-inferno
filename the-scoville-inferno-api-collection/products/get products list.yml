info:
  name: get products list
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​