info:
  name: get heat level list
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/heat-levels"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
