info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin1@example.com",
        "password":"Qwerty123!"
      }

runtime:
  variables:
    - name: BASE_URL
      value: http://localhost:3333/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
