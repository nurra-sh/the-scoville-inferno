info:
  name: get categories list
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
